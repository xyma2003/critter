"""
ui/tabs/pet.py — PetTab thin wrapper
All implementation lives in MainPanel._build_pet_tab().
"""


def build(parent, panel):
    """Build and return the pet tab frame."""
    return panel._build_pet_tab(parent)
