"""
ui/tabs/news.py — NewsTab thin wrapper
All implementation lives in MainPanel._build_news_tab().
"""


def build(parent, panel):
    """Build and return the news tab frame."""
    return panel._build_news_tab(parent)
