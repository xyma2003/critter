"""
ui/tabs/home.py — HomeTab thin wrapper
All implementation lives in MainPanel._build_home_tab().
"""


def build(parent, panel):
    """Build and return the home tab frame."""
    return panel._build_home_tab(parent)
