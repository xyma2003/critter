"""
ui/tabs/notes.py — NotesTab thin wrapper
All implementation lives in MainPanel._build_notes_tab().
"""


def build(parent, panel):
    """Build and return the notes tab frame."""
    return panel._build_notes_tab(parent)
