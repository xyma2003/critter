---
phase: 02-weather-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - services/weather/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_weather_service.py
autonomous: true
requirements:
  - WTHR-01
  - WTHR-02
  - WTHR-04
  - WTHR-05
  - WTHR-06

must_haves:
  truths:
    - "fetch_weather('Beijing', force=False) returns a dict with keys temp_C, feels_like_C, desc_zh, code, forecast"
    - "forecast is a list of exactly 3 dicts each with keys date, max_C, min_C, desc"
    - "A second call within 15 min with force=False returns (data, True, None) without making an HTTP request"
    - "fetch_weather with force=True bypasses cache and sets a new cache entry"
    - "fetch_weather for an unknown city returns (None, False, '找不到城市：...')"
    - "City names with spaces or Chinese characters are URL-encoded via urllib.parse.quote before insertion into the wttr.in URL"
  artifacts:
    - path: "config.py"
      provides: "WEATHER_FILE constant"
      contains: "WEATHER_FILE"
    - path: "services/weather/__init__.py"
      provides: "fetch_weather, is_cached, last_fetch_time, code_to_emoji, WEATHER_TTL"
      exports: ["fetch_weather", "is_cached", "last_fetch_time", "code_to_emoji", "WEATHER_TTL"]
    - path: "tests/test_weather_service.py"
      provides: "unit tests for WTHR-01 through WTHR-06 service layer"
      min_lines: 80
  key_links:
    - from: "services/weather/__init__.py"
      to: "wttr.in API"
      via: "urllib.request.urlopen with ssl.CERT_NONE context"
      pattern: "urlopen.*wttr\\.in"
    - from: "services/weather/__init__.py"
      to: "_cache dict"
      via: "time.time() TTL comparison"
      pattern: "_cache\\[city\\].*ts"
---

<objective>
Create the weather service module and its unit tests. This plan produces the complete fetch/cache layer that the UI plans will call — no UI code yet.

Purpose: Isolated network + cache layer follows services/news pattern; tests validate all six WTHR requirements at the service level before any UI is built.
Output: services/weather/__init__.py (fetch_weather, cache, emoji map), config.py WEATHER_FILE constant, tests/test_weather_service.py (mocked, no live network).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-weather-tab/02-RESEARCH.md
@config.py
@services/news/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WEATHER_FILE to config.py and create services/weather/__init__.py</name>
  <files>config.py, services/weather/__init__.py</files>
  <read_first>
    - config.py — existing constant patterns (BOOKMARKS_FILE pattern to mirror)
    - services/news/__init__.py — fetch/cache pattern to replicate
    - .planning/phases/02-weather-tab/02-RESEARCH.md — Pattern 1 (Weather Service Module) section, code examples section, pitfalls section
  </read_first>
  <behavior>
    - fetch_weather('Beijing', force=False) → (dict, False, None) on first call (cache miss)
    - fetch_weather('Beijing', force=False) → (same_dict, True, None) on second call within 15 min (cache hit, no HTTP)
    - fetch_weather('Beijing', force=True) → (dict, False, None) always goes to network
    - fetch_weather('Unknown_City_XYZ') → (None, False, str_starting_with '找不到城市：')
    - fetch_weather('上海') uses urllib.parse.quote(city, safe='') in URL construction
    - returned dict keys: temp_C (str), feels_like_C (str), desc_zh (str), code (int), forecast (list[3])
    - each forecast dict keys: date (str YYYY-MM-DD), max_C (str), min_C (str), desc (str)
    - is_cached(city) returns True only when cache entry exists and age < WEATHER_TTL (900)
    - last_fetch_time(city) returns float timestamp or None
    - code_to_emoji(113) == '☀️'; code_to_emoji(9999) == '🌡️' (fallback)
    - WEATHER_TTL == 900
  </behavior>
  <action>
    1. Open config.py. After the BOOKMARKS_FILE line, add:
       ```python
       WEATHER_FILE    = os.path.expanduser("~/.openclaw/workspace/desktop-pet/weather.json")
       ```

    2. Create services/weather/__init__.py with this exact structure:

       Module-level constants and state:
       - WEATHER_TTL = 900  (15 min, per WTHR-06)
       - _cache: dict = {}   # {city_name: {'data': dict, 'ts': float}}

       WEATHER_EMOJI dict (copy from RESEARCH.md Pattern 5 — all codes 113..395):
       ```python
       WEATHER_EMOJI = {
           113: '☀️', 116: '⛅', 119: '☁️', 122: '☁️',
           143: '🌫️', 176: '🌦️', 179: '🌨️', 200: '⛈️',
           248: '🌫️', 260: '🌫️', 263: '🌦️', 266: '🌧️',
           293: '🌦️', 296: '🌧️', 302: '🌧️', 308: '🌧️',
           317: '🌨️', 320: '🌨️', 323: '🌨️', 326: '🌨️',
           338: '❄️', 353: '🌦️', 356: '🌧️', 368: '🌨️',
           371: '❄️', 386: '⛈️', 389: '⛈️', 395: '⛈️',
       }
       ```

       def code_to_emoji(code: int) -> str:
           return WEATHER_EMOJI.get(int(code), '🌡️')

       def _build_ssl_ctx():
           ctx = ssl.create_default_context()
           ctx.check_hostname = False
           ctx.verify_mode = ssl.CERT_NONE
           return ctx

       def fetch_weather(city: str, force: bool = False) -> tuple[dict|None, bool, str|None]:
           """Returns (data_dict, from_cache, error_msg). data_dict is None on error."""
           cached = _cache.get(city)
           if cached and not force and (time.time() - cached['ts'] < WEATHER_TTL):
               return cached['data'], True, None

           encoded = urllib.parse.quote(city, safe='')
           url = f'https://wttr.in/{encoded}?format=j1&lang=zh'
           req = urllib.request.Request(url, headers={'User-Agent': 'Critter/1.0'})
           try:
               with urllib.request.urlopen(req, context=_build_ssl_ctx(), timeout=10) as resp:
                   raw = json.loads(resp.read().decode('utf-8'))
           except urllib.error.HTTPError as e:
               body = e.read().decode('utf-8', errors='replace')
               if 'location not found' in body.lower():
                   return None, False, f'找不到城市：{city}'
               return None, False, f'请求失败 ({e.code})'
           except urllib.error.URLError as e:
               return None, False, f'网络错误：{e.reason}'
           except json.JSONDecodeError:
               return None, False, '解析失败'

           cc = raw['current_condition'][0]
           lang_zh_list = cc.get('lang_zh') or []
           desc_zh = lang_zh_list[0].get('value', '') if lang_zh_list else (
               (cc.get('weatherDesc') or [{}])[0].get('value', ''))

           forecast = []
           for day in raw.get('weather', [])[:3]:
               hourly = day.get('hourly', [])
               idx = 4 if len(hourly) > 4 else (len(hourly) - 1 if hourly else 0)
               if hourly and idx >= 0:
                   lang_list = hourly[idx].get('lang_zh') or hourly[idx].get('weatherDesc') or []
               else:
                   lang_list = []
               day_desc = lang_list[0].get('value', '') if lang_list else ''
               forecast.append({
                   'date':  day['date'],
                   'max_C': day['maxtempC'],
                   'min_C': day['mintempC'],
                   'desc':  day_desc,
               })

           data = {
               'temp_C':       cc['temp_C'],
               'feels_like_C': cc['FeelsLikeC'],
               'desc_zh':      desc_zh,
               'code':         int(cc.get('weatherCode', 0)),
               'forecast':     forecast,
           }
           _cache[city] = {'data': data, 'ts': time.time()}
           return data, False, None

       def is_cached(city: str) -> bool:
           c = _cache.get(city)
           return bool(c and (time.time() - c['ts'] < WEATHER_TTL))

       def last_fetch_time(city: str) -> float | None:
           c = _cache.get(city)
           return c['ts'] if c else None

       Imports at top of services/weather/__init__.py:
       import json, ssl, time, urllib.error, urllib.parse, urllib.request
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "from services.weather import fetch_weather, is_cached, last_fetch_time, code_to_emoji, WEATHER_TTL; print('imports OK'); assert WEATHER_TTL == 900; assert code_to_emoji(113) == '☀️'; assert code_to_emoji(9999) == '🌡️'; print('constants OK')"</automated>
  </verify>
  <done>services/weather/__init__.py importable; WEATHER_TTL==900; code_to_emoji(113)=='☀️'; WEATHER_FILE in config.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for weather service (mocked network)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_weather_service.py</files>
  <read_first>
    - services/weather/__init__.py — the module just created: function signatures, _cache dict, WEATHER_TTL
    - .planning/phases/02-weather-tab/02-RESEARCH.md — Validation Architecture section, Phase Requirements → Test Map
  </read_first>
  <behavior>
    - test_parse_current: mock urlopen to return fixture JSON with current_condition[0]; assert result dict has temp_C, feels_like_C, desc_zh, code, forecast
    - test_chinese_city: mock urlopen; assert urllib.parse.quote called with '上海' and safe=''; returned data is valid
    - test_cache_hit: call fetch_weather once (mock), call again with force=False; assert urlopen called exactly once
    - test_cache_ttl: call fetch_weather (mock), monkey-patch cached ts to time.time()-901; call again force=False; assert urlopen called twice
    - test_forecast_three_days: mock returns 3 weather days; assert len(result['forecast']) == 3
    - test_cache_miss_on_force: populate cache manually; call fetch_weather(city, force=True); assert urlopen called (not short-circuited)
    - test_unknown_city_error: mock urlopen to raise HTTPError(500) with body 'location not found: xyz'; assert return is (None, False, '找不到城市：xyz')
    - test_code_to_emoji: assert code_to_emoji(113)=='☀️', code_to_emoji(200)=='⛈️', code_to_emoji(0)=='🌡️'
  </behavior>
  <action>
    1. Create tests/__init__.py (empty file — makes tests a package)

    2. Create tests/conftest.py with a shared fixture:
       ```python
       import json
       import pytest

       MOCK_WTTR_RESPONSE = {
           "current_condition": [{
               "temp_C": "18",
               "FeelsLikeC": "17",
               "weatherCode": "116",
               "lang_zh": [{"value": "部分多云"}],
               "weatherDesc": [{"value": "Partly cloudy"}],
               "humidity": "60",
           }],
           "weather": [
               {
                   "date": "2026-04-22",
                   "maxtempC": "22",
                   "mintempC": "14",
                   "hourly": [
                       {}, {}, {}, {},
                       {"lang_zh": [{"value": "多云"}], "weatherDesc": [{"value": "Cloudy"}]},
                   ],
               },
               {
                   "date": "2026-04-23",
                   "maxtempC": "25",
                   "mintempC": "15",
                   "hourly": [
                       {}, {}, {}, {},
                       {"lang_zh": [{"value": "晴"}], "weatherDesc": [{"value": "Sunny"}]},
                   ],
               },
               {
                   "date": "2026-04-24",
                   "maxtempC": "23",
                   "mintempC": "13",
                   "hourly": [
                       {}, {}, {}, {},
                       {"lang_zh": [{"value": "阵雨"}], "weatherDesc": [{"value": "Showers"}]},
                   ],
               },
           ],
       }

       @pytest.fixture
       def mock_wttr_bytes():
           return json.dumps(MOCK_WTTR_RESPONSE).encode('utf-8')
       ```

    3. Create tests/test_weather_service.py. Clear the services.weather._cache between tests using a fixture that sets `import services.weather as svc; svc._cache.clear()`.

       Use unittest.mock.patch('urllib.request.urlopen') to mock HTTP. The mock context manager's __enter__ should return an object whose .read() returns mock_wttr_bytes.

       Test functions:
       - test_parse_current(mock_urlopen, mock_wttr_bytes): calls fetch_weather('Beijing'); asserts data['temp_C']=='18', data['feels_like_C']=='17', data['desc_zh']=='部分多云', data['code']==116, len(data['forecast'])==3
       - test_chinese_city(mock_urlopen, mock_wttr_bytes): calls fetch_weather('上海'); assert urlopen called; check called URL contains '%E4%B8%8A%E6%B5%B7' (URL-encoded 上海)
       - test_cache_hit(mock_urlopen, mock_wttr_bytes): call twice with force=False; assert mock_urlopen.call_count==1; second return has from_cache==True
       - test_cache_ttl(monkeypatch, mock_urlopen, mock_wttr_bytes): call once; monkeypatch cache ts to time.time()-901; call again; assert mock_urlopen.call_count==2
       - test_forecast_three_days(mock_urlopen, mock_wttr_bytes): assert len(result['forecast'])==3; each item has 'date','max_C','min_C','desc'
       - test_cache_miss_on_force(mock_urlopen, mock_wttr_bytes): call twice with force=True both times; assert mock_urlopen.call_count==2
       - test_unknown_city_error(mock_urlopen): mock raises urllib.error.HTTPError with code=500, body b'location not found: xyz'; assert fetch_weather('xyz') == (None, False, '找不到城市：xyz')
       - test_code_to_emoji(): from services.weather import code_to_emoji; assert code_to_emoji(113)=='☀️'; assert code_to_emoji(200)=='⛈️'; assert code_to_emoji(0)=='🌡️'

    Install pytest if not present:
    /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -m pip install pytest --quiet
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -m pytest tests/test_weather_service.py -x -q 2>&1</automated>
  </verify>
  <done>All 8 tests in test_weather_service.py pass with no live network calls; pytest exits 0</done>
</task>

</tasks>

<verification>
1. `python3.11 -c "from services.weather import fetch_weather, is_cached, last_fetch_time, code_to_emoji, WEATHER_TTL; from config import WEATHER_FILE"` exits 0
2. `grep -n "WEATHER_FILE" config.py` shows the constant
3. `python3.11 -m pytest tests/test_weather_service.py -v` shows 8 passed
4. `grep -n "urllib.parse.quote" services/weather/__init__.py` confirms URL encoding
5. `grep -n "CERT_NONE" services/weather/__init__.py` confirms SSL pattern
</verification>

<success_criteria>
- services/weather/__init__.py exists with fetch_weather, is_cached, last_fetch_time, code_to_emoji, WEATHER_TTL=900, WEATHER_EMOJI dict
- config.py has WEATHER_FILE constant pointing to ~/.openclaw/workspace/desktop-pet/weather.json
- tests/test_weather_service.py has 8 tests, all green, zero live HTTP calls
- All WTHR-01, WTHR-02, WTHR-04, WTHR-05, WTHR-06 service-level behaviors covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-weather-tab/02-01-SUMMARY.md`
</output>
