---
phase: quick
plan: 260424-eu6
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/panel.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "天气 Tab 当前天气卡片下方显示一条宠物口吻的穿搭建议"
    - "切换城市或刷新天气时穿搭建议自动更新"
    - "Claude 生成中显示 loading 旋转动画；生成完成后替换为建议文字"
    - "同 city+temp 组合不重复调用 Claude（内存缓存）"
  artifacts:
    - path: "ui/panel.py"
      provides: "_fetch_outfit_advice_async, _render_outfit_card"
      contains: "_outfit_cache"
  key_links:
    - from: "_render_current_weather"
      to: "_fetch_outfit_advice_async"
      via: "called after current weather card is rendered"
    - from: "_fetch_outfit_advice_async (background thread)"
      to: "_render_outfit_card (main thread)"
      via: "root.after(0, callback)"
---

<objective>
在天气 Tab 当前天气卡片下方，调用 Claude CLI 生成一句宠物口吻的穿搭建议并渲染。

Purpose: 增加天气 Tab 的实用性和宠物感——天气数据冷冰冰，穿搭建议让数据"活"起来。
Output: `_outfit_cache` 内存缓存 + `_fetch_outfit_advice_async` 后台调用 + `_render_outfit_card` UI 渲染（含 loading 状态）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260424-eu6-tab-claude-loading/260424-eu6-PLAN.md

## Key interfaces

### services/weather/__init__.py — data dict structure
```python
data = {
    'temp_C':       str,   # e.g. "12"
    'feels_like_C': str,
    'desc_zh':      str,   # e.g. "多云"
    'code':         int,
    'forecast':     list,
}
```

### config.py
```python
CLAUDE_CLI = '/opt/homebrew/bin/claude'
```

### 已有 Claude 调用模式（来自 panel.py diary 功能）
```python
proc = subprocess.Popen(
    [CLAUDE_CLI, '--print', '--output-format', 'json'],
    stdin=subprocess.PIPE, stdout=subprocess.PIPE,
    stderr=subprocess.PIPE, text=True
)
out, _ = proc.communicate(input=prompt, timeout=30)
result = json.loads(out.strip())['result']
```

### _render_current_weather 渲染顺序（当前 panel.py）
1. 清空 _weather_main_frame
2. 若 data 存在：渲染 current conditions card（card.pack padx=20 pady=(20,10)）
3. City label（padx=20）
4. _render_forecast(data)

穿搭建议卡片插入在 city label 之后、_render_forecast 之前。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 _outfit_cache 及 _fetch_outfit_advice_async 后台调用</name>
  <files>ui/panel.py</files>
  <action>
**1. 初始化缓存（__init__ 方法，紧跟 _weather_fetching 初始化之后）**

```python
self._outfit_cache: dict = {}   # key: f"{city}:{temp_C}" -> str（建议文字）
```

**2. 新增 `_fetch_outfit_advice_async` 方法**（放在 `_load_weather_async` 附近）

```python
def _fetch_outfit_advice_async(self, city: str, temp_C: str, desc_zh: str, on_done):
    """在后台线程调用 Claude CLI 生成穿搭建议，完成后通过 on_done(text) 回调到主线程。
    on_done 在主线程安全，直接操作 tkinter widget。
    """
    cache_key = f"{city}:{temp_C}"
    if cache_key in self._outfit_cache:
        self._root.after(0, lambda: on_done(self._outfit_cache[cache_key]))
        return

    def _run():
        prompt = (
            f"你是一只可爱的猫咪宠物，用第一人称、宠物口吻，"
            f"根据当前天气（{temp_C}°C，{desc_zh}）给主人一句简短的穿搭建议。"
            f"只输出一句话，20字以内，不要换行，结尾可加一个可爱的颜文字或emoji。"
        )
        try:
            proc = subprocess.Popen(
                [CLAUDE_CLI, '--print', '--output-format', 'json'],
                stdin=subprocess.PIPE, stdout=subprocess.PIPE,
                stderr=subprocess.PIPE, text=True
            )
            out, _ = proc.communicate(input=prompt, timeout=30)
            data = json.loads(out.strip())
            text = data.get('result', '').strip()
        except Exception:
            text = ''
        if text:
            self._outfit_cache[cache_key] = text
        self._root.after(0, lambda: on_done(text))

    threading.Thread(target=_run, daemon=True).start()
```

注意：`self._root` 是 MainPanel 对 tk root 的引用（变量名参照 panel.py 中现有写法，若是 `self.root` 则使用 `self.root`，grep 后确认）。
  </action>
  <verify>python3.11 -c "import ast; ast.parse(open('ui/panel.py').read()); print('syntax OK')"</verify>
  <done>panel.py 语法正确，`_outfit_cache` 在 __init__ 中初始化，`_fetch_outfit_advice_async` 方法存在</done>
</task>

<task type="auto">
  <name>Task 2: 在 _render_current_weather 插入穿搭建议卡片（含 loading 状态）</name>
  <files>ui/panel.py</files>
  <action>
在 `_render_current_weather` 方法中，city label 的 pack 语句之后、`self._render_forecast(data)` 调用之前，插入以下代码：

```python
# ── 穿搭建议卡片 ──
outfit_card = tk.Frame(self._weather_main_frame, bg=th['BG_CARD'],
                       highlightthickness=1, highlightbackground=th['BORDER'])
outfit_card.pack(fill=tk.X, padx=20, pady=(8, 0))

outfit_lbl = tk.Label(outfit_card, text='⏳  生成中…', bg=th['BG_CARD'],
                      fg=th['FG_MUTED'], font=('PingFang SC', 12),
                      anchor='w', wraplength=560, justify='left')
outfit_lbl.pack(fill=tk.X, padx=14, pady=10)

# loading 旋转动画
_FRAMES = ('⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏')
_fidx = [0]
_spinning = [True]

def _spin():
    if not _spinning[0]:
        return
    try:
        outfit_lbl.configure(text=f'{_FRAMES[_fidx[0]]}  生成中…')
    except tk.TclError:
        return
    _fidx[0] = (_fidx[0] + 1) % len(_FRAMES)
    outfit_lbl.after(100, _spin)

_spin()

def _on_advice(text, lbl=outfit_lbl):
    _spinning[0] = False
    try:
        lbl.configure(
            text=text if text else '今天天气不错，出门记得看看穿搭哦～',
            fg=th['FG_MAIN']
        )
    except tk.TclError:
        pass

self._fetch_outfit_advice_async(city, data['temp_C'], data['desc_zh'], _on_advice)
```

**位置参考**（精确插入点）：

```python
        # City label — 现有代码
        tk.Label(self._weather_main_frame, text=city, ...).pack(anchor='w', padx=20)
        # ← 在这里插入穿搭建议卡片代码
        self._render_forecast(data)   # 现有代码
```
  </action>
  <verify>python3.11 -c "import ast; ast.parse(open('ui/panel.py').read()); print('syntax OK')"</verify>
  <done>
  - `_render_current_weather` 在 city label 之后、forecast 之前渲染穿搭建议卡片
  - 卡片初始显示 loading 旋转动画（Braille spinner）
  - Claude 返回文字后替换为建议文字
  - 若调用失败则显示兜底文案
  - 同 city+temp 不重复调用（缓存命中时直接回调）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>天气 Tab 穿搭建议：当前天气卡片下方新增卡片，先显示 spinner loading，Claude 返回后显示宠物口吻建议；同 city+temp 切换时命中缓存直接显示。</what-built>
  <how-to-verify>
1. 启动应用：`/Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 main.py`
2. 打开主面板，切换到天气 Tab
3. 若已有城市：观察当前天气卡片下方出现 loading spinner，等待约 5-15 秒后替换为一句宠物口吻建议文字
4. 点击手动刷新按钮（右上角）：应再次触发建议更新（temp 未变化时命中缓存立即显示）
5. 若有多个城市，切换城市：新城市应显示 loading → 建议
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
python3.11 -c "import ast; ast.parse(open('ui/panel.py').read()); print('syntax OK')"
</verification>

<success_criteria>
- 天气 Tab 当前天气卡片下方显示穿搭建议卡片
- 生成中显示 Braille spinner loading 动画
- Claude 返回后显示宠物口吻的穿搭建议（20 字以内，带可爱颜文字）
- 切换城市触发新建议；同 city+temp 命中缓存不重复调用
- 调用失败时显示兜底文案，不崩溃
</success_criteria>

<output>
完成后创建 `.planning/quick/260424-eu6-tab-claude-loading/260424-eu6-SUMMARY.md`
</output>
