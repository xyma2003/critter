---
phase: quick
plan: 260424-f5g
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/panel.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "日记卡片 × 按钮在右上角，不与卡片文字重叠"
    - "导航栏第6个图标为 📖，点击进入日记 Tab"
    - "日记 Tab 按日期倒序列出所有 kind==diary 的记录，每条显示日期标题+内容预览"
    - "点击日记条目弹出只读全文视图（复用 _notes_open_readonly）"
    - "从宠物 Tab 切走时后台重新生成当天日记，日记 Tab 显示「正在更新…」直到完成"
    - "便签 Tab 不再显示 kind==diary 的条目"
  artifacts:
    - path: "ui/panel.py"
      provides: "主面板，包含新日记 Tab 和所有修复"
  key_links:
    - from: "_switch_tab"
      to: "_rebuild_diary_tab"
      via: "检测 prev_tab=='pet'，后台重新生成日记"
    - from: "_notes_show_list"
      to: "notes_service.load_all()"
      via: "过滤掉 kind=='diary' 的条目"
---

<objective>
宠物日记重构：修复卡片删除按钮错位、新增独立日记 Tab、离开宠物 Tab 自动刷新日记、便签 Tab 过滤日记条目。

Purpose: 日记与便签在 UI 上彻底分离，用户体验更清晰。
Output: ui/panel.py 包含上述4项改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260424-f5g-tab-tab/260424-f5g-PLAN.md

关键现有代码摘要（已读取）：

tab_defs 目前6项（含 settings）：home/news/pet/notes/weather/settings。
新增 diary Tab 需插入 weather 之后、settings 之前，即第6位（导航栏第6个图标）。

_make_card 中删除按钮：
  del_btn.place(x=CARD_W-20, y=2)   # CARD_W=160，所以 x=140 y=2
  卡片 height=120，title label: place(x=8, y=title_y, width=144, height=64)
  → 按钮 x=140 与 title label 右边界重叠，是错位根因。

_switch_tab 中：目前没有记录 prev_tab，需要在方法开头保存。

_notes_show_list 中：
  notes = notes_service.load_all()
  → 需改为过滤 kind=='diary'。

services/diary generate_diary(stats_snap, counts, pet_name, pet_personality, pet_catchphrase, today) → str | None
notes_service.create_diary(content, date_str) 创建 kind=='diary' 记录
notes_service.load_all() 返回全部 note 列表（含 kind 字段）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复日记卡片 × 按钮错位</name>
  <files>ui/panel.py</files>
  <action>
在 _make_card 函数中，将删除按钮改为覆盖在卡片右上角，与文字内容区不重叠：

当前问题：
  del_btn.place(x=CARD_W-20, y=2)
  卡片宽度160，title label 从 x=8 延伸到 x=152（width=CARD_W-16=144），del_btn 的 x=140 落在文字区内。

修复方案：
  将 del_btn 改为 place(relx=1.0, rely=0.0, anchor='ne', x=-4, y=4)
  这样按钮锚定在右上角，距边缘4px，与下方文字完全分离。

同时把 del_btn 的字体改小一号：font=('PingFang SC', 12, 'bold')（当前是14）。
保持所有 bind 事件不变。

只修改 del_btn.place(...) 这一行（约2097行附近的 _make_card 内部）。
  </action>
  <verify>运行 /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast, sys; ast.parse(open('ui/panel.py').read()); print('syntax OK')" 在 /Users/maxinyue09/.openclaw/workspace/desktop-pet/ 目录下</verify>
  <done>del_btn.place 改为 relx/rely anchor 方式，语法检查通过</done>
</task>

<task type="auto">
  <name>Task 2: 新增日记 Tab（导航 + 构建方法）</name>
  <files>ui/panel.py</files>
  <action>
步骤 A — tab_defs 插入日记入口：
在 _build 方法中的 tab_defs 列表里，在 ('weather', '🌤', '天气') 之后、('settings', '⚙️', '设置') 之前插入：
  ('diary', '📖', '日记'),

步骤 B — 构建 tab frame 和 grid 注册：
在 self._tab_frames['weather'] = self._build_weather_tab(...) 之后插入：
  self._tab_frames['diary'] = self._build_diary_tab(self._content_host)
并在 for frame in self._tab_frames.values(): frame.grid(...) 之前确保 diary frame 已在 dict 中（它会自动被 grid）。

步骤 C — 新增实例变量（在 __init__ 末尾追加）：
  self._diary_loading = False    # 正在后台重新生成日记

步骤 D — 新增 _build_diary_tab 方法，紧接在 _build_notes_tab 之前（约1958行附近），内容如下：

```python
def _build_diary_tab(self, parent):
    """Tab: 日记 — 按日期倒序展示所有 kind==diary 记录。"""
    th = THEMES[self._theme_mode]
    frame = tk.Frame(parent, bg=th['BG_CONTENT'])

    toolbar = tk.Frame(frame, bg=th['BG_TOOLBAR'], height=44)
    toolbar.pack(fill=tk.X)
    toolbar.pack_propagate(False)
    tk.Label(toolbar, text='宠物日记',
             bg=th['BG_TOOLBAR'], fg=th['FG_MAIN'],
             font=('PingFang SC', 13, 'bold')).pack(side=tk.LEFT, padx=16)

    self._diary_status = tk.Label(toolbar, text='', bg=th['BG_TOOLBAR'],
                                  fg=th['FG_MUTED'], font=('PingFang SC', 10))
    self._diary_status.pack(side=tk.LEFT, padx=4)

    tk.Frame(frame, bg=th['DIVIDER'], height=1).pack(fill=tk.X)

    self._diary_list_host = tk.Frame(frame, bg=th['BG_CONTENT'])
    self._diary_list_host.pack(fill=tk.BOTH, expand=True)

    self._diary_readonly_text = tk.Text(
        self._diary_list_host, bg=th['BG_CARD'], fg=th['FG_MAIN'],
        font=('PingFang SC', 13), relief=tk.FLAT, padx=20, pady=16,
        wrap=tk.WORD, state=tk.DISABLED,
        insertbackground=th['FG_ACCENT'],
        selectbackground=th['BG_SEL'],
        borderwidth=0
    )
    self._diary_back_btn = tk.Label(toolbar, text='← 返回', bg=th['BG_TOOLBAR'],
                                    fg=th['FG_MAIN'], font=('PingFang SC', 11),
                                    cursor='hand2', padx=10)
    self._diary_back_btn.bind('<Button-1>', lambda e: self._diary_show_list())
    self._diary_back_btn.bind('<Enter>', lambda e: self._diary_back_btn.configure(fg=th['FG_ACCENT']))
    self._diary_back_btn.bind('<Leave>', lambda e: self._diary_back_btn.configure(fg=th['FG_MAIN']))

    self._diary_title_label = toolbar.children.get('!label') or tk.Label(toolbar, text='宠物日记')
    # 用 toolbar 里第一个 Label 作为标题引用，通过 _diary_title_str 存储
    self._diary_title_str = '宠物日记'

    self._diary_show_list()
    return frame
```

步骤 E — 新增 _diary_show_list 方法（在 _build_diary_tab 之后）：

```python
def _diary_show_list(self):
    """日记 Tab 的列表视图（按日期倒序）。"""
    th = THEMES[self._theme_mode]
    self._diary_readonly_text.pack_forget()
    self._diary_back_btn.pack_forget()
    self._diary_status.configure(text='正在更新…' if getattr(self, '_diary_loading', False) else '')

    if hasattr(self, '_diary_list_frame') and self._diary_list_frame:
        self._diary_list_frame.destroy()

    all_notes = notes_service.load_all()
    diaries = sorted(
        [n for n in all_notes if n.get('kind') == 'diary'],
        key=lambda n: n.get('date', ''),
        reverse=True
    )

    outer = tk.Frame(self._diary_list_host, bg=th['BG_CONTENT'])
    outer.pack(fill=tk.BOTH, expand=True)
    self._diary_list_frame = outer

    _, inner = self._make_scrollable_frame(outer, th['BG_CONTENT'])

    if not diaries:
        tk.Label(inner, text='还没有日记哦，去和宠物互动后再来看看~',
                 bg=th['BG_CONTENT'], fg=th['FG_MUTED'],
                 font=('PingFang SC', 12)).pack(pady=40)
        return

    for diary in diaries:
        card = tk.Frame(inner, bg=th['BG_CARD'],
                        highlightbackground=th['BORDER'],
                        highlightthickness=1,
                        cursor='hand2')
        card.pack(fill=tk.X, padx=16, pady=6)

        date_str = diary.get('date', '')
        try:
            import datetime as _datetime
            d = _datetime.date.fromisoformat(date_str)
            date_label = f"{d.year}年{d.month}月{d.day}日"
        except Exception:
            date_label = date_str

        header = tk.Frame(card, bg=th['BG_CARD'])
        header.pack(fill=tk.X, padx=12, pady=(10, 4))
        tk.Label(header, text='📖 ' + date_label,
                 bg=th['BG_CARD'], fg=th['FG_ACCENT'],
                 font=('PingFang SC', 11, 'bold')).pack(side=tk.LEFT)

        _plain = self._strip_markdown(diary['content']).replace('\n', ' ')
        preview = (_plain[:60] + '…') if len(_plain) > 60 else _plain
        tk.Label(card, text=preview,
                 bg=th['BG_CARD'], fg=th['FG_MAIN'],
                 font=('PingFang SC', 11),
                 wraplength=880, justify=tk.LEFT, anchor='w').pack(
                     fill=tk.X, padx=12, pady=(0, 10))

        def _open(e, nid=diary['id'], ds=date_str):
            self._diary_open_readonly(nid, ds)
        def _enter(e, c=card):
            c.configure(highlightbackground=th['FG_ACCENT'])
        def _leave(e, c=card):
            c.configure(highlightbackground=th['BORDER'])

        card.bind('<Button-1>', _open)
        for child in card.winfo_children():
            child.bind('<Button-1>', _open)
            for grandchild in child.winfo_children():
                grandchild.bind('<Button-1>', _open)
        card.bind('<Enter>', _enter)
        card.bind('<Leave>', _leave)
```

步骤 F — 新增 _diary_open_readonly 方法（在 _diary_show_list 之后）：

```python
def _diary_open_readonly(self, note_id, date_str):
    """展开单条日记的只读视图。"""
    th = THEMES[self._theme_mode]
    all_notes = notes_service.load_all()
    content = ''
    for n in all_notes:
        if n['id'] == note_id:
            content = n['content']
            break

    if hasattr(self, '_diary_list_frame') and self._diary_list_frame:
        self._diary_list_frame.pack_forget()

    try:
        import datetime as _datetime
        d = _datetime.date.fromisoformat(date_str)
        date_label = f"{d.year}年{d.month}月{d.day}日"
    except Exception:
        date_label = date_str

    self._diary_status.configure(text=date_label)
    self._diary_back_btn.pack(side=tk.LEFT, pady=4)

    self._diary_readonly_text.pack(fill=tk.BOTH, expand=True)
    self._render_markdown_in_widget(self._diary_readonly_text, content)
```

注意：_render_markdown_in_widget 现有实现已经在便签 Tab 中使用（约2242行），可直接复用。
  </action>
  <verify>运行 /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast, sys; ast.parse(open('ui/panel.py').read()); print('syntax OK')" 在 /Users/maxinyue09/.openclaw/workspace/desktop-pet/ 目录下</verify>
  <done>导航栏包含 📖 日记按钮；_build_diary_tab / _diary_show_list / _diary_open_readonly 方法存在；语法检查通过</done>
</task>

<task type="auto">
  <name>Task 3: 离开宠物 Tab 自动重新生成日记 + 便签 Tab 过滤日记</name>
  <files>ui/panel.py</files>
  <action>
改动 A — _switch_tab 记录 prev_tab 并触发日记重新生成：

在 _switch_tab 方法开头（即 self._active_tab = key 之前）加入：
  prev_tab = getattr(self, '_active_tab', None)

然后在方法末尾（所有 if key == '...' 块之后）追加：

```python
# 从宠物 Tab 切走：后台重新生成当天日记
if prev_tab == 'pet' and key != 'pet':
    self._regen_diary_on_leave_pet()
```

改动 B — 新增 _regen_diary_on_leave_pet 方法（在 _check_and_generate_diary 附近）：

```python
def _regen_diary_on_leave_pet(self):
    """离开宠物 Tab 时，后台重新生成当天日记（替换旧记录）。"""
    today = self._today_str()
    counts = self._get_diary_counts_today()
    stats_snap = {
        'mood': round(self.stats.mood, 1),
        'hunger': round(self.stats.hunger, 1),
        'energy': round(self.stats.energy, 1),
        'mood_label': self.stats.mood_label(),
    }
    pet_name = self.pet.settings.get('pet_name', '小猫')
    pet_personality = self.pet.settings.get('pet_personality', '温柔')
    pet_catchphrase = self.pet.settings.get('pet_catchphrase', '喵~')

    # 标记 loading，刷新日记 Tab 状态栏
    self._diary_loading = True
    if self.win and self.win.winfo_exists():
        try:
            self._diary_status.configure(text='正在更新…')
        except Exception:
            pass

    def _gen():
        try:
            import services.diary as diary_service
            text = diary_service.generate_diary(
                stats_snap, counts, pet_name, pet_personality, pet_catchphrase, today
            )
            if text:
                # 删除今天的旧日记（如有），然后创建新的
                all_notes = notes_service.load_all()
                remaining = [
                    n for n in all_notes
                    if not (n.get('kind') == 'diary' and n.get('date') == today)
                ]
                notes_service.save_all(remaining)
                notes_service.create_diary(text, today)
        except Exception:
            pass
        finally:
            self._diary_loading = False
            if self.win and self.win.winfo_exists():
                self.win.after(0, self._diary_refresh_if_active)

    threading.Thread(target=_gen, daemon=True).start()

def _diary_refresh_if_active(self):
    """日记生成完毕后，若当前在日记 Tab 则刷新列表。"""
    if getattr(self, '_active_tab', None) == 'diary':
        self._diary_show_list()
    else:
        # 更新状态标签（可能日记 Tab 在后台）
        try:
            self._diary_status.configure(text='')
        except Exception:
            pass
```

改动 C — _notes_show_list 过滤日记条目：

找到 _notes_show_list 中的：
  notes = notes_service.load_all()
改为：
  notes = [n for n in notes_service.load_all() if n.get('kind') != 'diary']

这样便签 Tab 的列表、网格、计数都只含普通便签。同时在 _build_notes_tab 初始化时：
  if notes_service.load_all():
      self._notes_show_list()
保持不变（_notes_show_list 内部已过滤，不影响逻辑）。

注意：_diary_show_list 在 diary Tab build 时调用，此时 _diary_list_frame 属性还未设置，需在方法开头用 hasattr 检查，已在 Task 2 中处理。
  </action>
  <verify>运行 /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast, sys; ast.parse(open('ui/panel.py').read()); print('syntax OK')" 在 /Users/maxinyue09/.openclaw/workspace/desktop-pet/ 目录下；然后启动应用确认：1) 切换到宠物 Tab 再切走 → 日记 Tab 出现「正在更新…」 2) 便签 Tab 不显示日记卡片</verify>
  <done>
  - _switch_tab 检测 prev_tab=='pet' 并调用 _regen_diary_on_leave_pet
  - _regen_diary_on_leave_pet 后台替换当天日记，完成后刷新日记 Tab
  - _notes_show_list 过滤 kind=='diary' 条目
  - 语法检查通过
  </done>
</task>

</tasks>

<verification>
语法检查（每任务完成后）：
  cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast; ast.parse(open('ui/panel.py').read()); print('OK')"

启动测试（全部完成后）：
  cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 main.py

验证清单：
- [ ] 便签卡片 × 按钮位于右上角，不压在文字上
- [ ] 导航栏第6个图标为 📖（weather 之后 settings 之前）
- [ ] 日记 Tab 按日期倒序列出 kind==diary 条目
- [ ] 点击日记条目展开只读视图，← 返回可回到列表
- [ ] 从宠物 Tab 切走后日记 Tab 状态栏显示「正在更新…」，完成后消失
- [ ] 便签 Tab 不显示 kind==diary 的卡片
</verification>

<success_criteria>
1. del_btn 不与任何卡片文字重叠（place 使用 relx=1.0, rely=0.0, anchor='ne'）
2. 导航栏有 📖 日记按钮，点击后进入独立日记 Tab
3. 日记 Tab 列出所有 kind==diary 记录，日期倒序，可点击查看全文
4. 离开宠物 Tab → 后台重新生成当天日记 → 完成后日记 Tab 自动刷新
5. 便签 Tab 只显示 kind!=diary 的普通便签
</success_criteria>

<output>
任务完成后创建 .planning/quick/260424-f5g-tab-tab/260424-f5g-SUMMARY.md
</output>
