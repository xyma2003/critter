---
phase: quick
plan: 260424-g7w
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/panel.py
autonomous: true
requirements:
  - NOTES-WYSIWYG-01
must_haves:
  truths:
    - "打开便签编辑视图时，左右各约 50% 宽度：左侧 Markdown 编辑框，右侧实时预览"
    - "工具栏显示 B / I / U / H1 / H2 / 无序 / 有序 七个快捷按钮"
    - "有选中文字时点击工具栏按钮，选中文字被包裹上对应语法；无选中时在光标处插入占位符"
    - "每次按键后预览区立即刷新（KeyRelease 绑定）"
    - "编辑器中不再有「👁 预览 / ✏️ 编辑」切换按钮"
    - "日记只读视图（_notes_open_readonly）外观和行为不受影响"
  artifacts:
    - path: "ui/panel.py"
      provides: "WYSIWYG 分栏编辑器"
      contains: "_notes_open_editor"
  key_links:
    - from: "editor (tk.Text, 左侧)"
      to: "preview (tk.Text, 右侧)"
      via: "KeyRelease 绑定 _refresh_preview()"
      pattern: "KeyRelease.*_refresh_preview"
    - from: "工具栏按钮"
      to: "editor"
      via: "_notes_toolbar_wrap(tag) 插入/包裹文字"
      pattern: "_notes_toolbar_wrap"
---

<objective>
将便签编辑视图改为左右分栏 WYSIWYG 布局：左侧 Markdown 编辑框 + 顶部格式工具栏，右侧实时预览。

Purpose: 用户写 Markdown 时能即时看到渲染效果，无需手动切换预览模式。
Output: `ui/panel.py` 中 `_notes_open_editor` 重构为分栏布局，新增 `_notes_toolbar_wrap` 辅助方法，删除旧的 `_notes_toggle_preview` 逻辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260424-g7w-markdown-wysiwyg/260424-g7w-PLAN.md

<interfaces>
<!-- Key existing methods and attributes the executor needs. No exploration required. -->

From ui/panel.py — 现有便签相关属性（在 _build_notes_tab 中初始化）:
  self._notes_body         : tk.Frame  — 工具栏下方的主体容器
  self._notes_text         : tk.Text   — 现有单一编辑器 widget（需替换为左侧编辑器）
  self._notes_current_id   : str|None
  self._notes_mode         : 'list' | 'edit'
  self._notes_previewing   : bool      — 将被移除
  self._notes_list_frame   : tk.Frame|None
  self._notes_title_label  : tk.Label
  self._notes_status       : tk.Label
  self._notes_save_btn     : tk.Label  — 保留，继续 pack(side=RIGHT)
  self._notes_preview_btn  : tk.Label  — 移除（不再 pack）
  self._notes_back_btn     : tk.Label  — 保留

From ui/panel.py — 现有渲染方法:
  self._render_markdown_in_widget(widget, text: str)
    — 将 Markdown 写入只读 tk.Text；内部调用 widget.configure(state=DISABLED)

From ui/panel.py — _notes_open_readonly（只读日记，不改动）:
  self._notes_text.pack(fill=tk.BOTH, expand=True)
  self._render_markdown_in_widget(self._notes_text, content)
  NOTE: _notes_open_readonly 复用 self._notes_text，因此重构后 self._notes_text
        需要保持存在并在 readonly 视图时仍可 pack 进 self._notes_body。

Theme keys used:
  th['BG_TOOLBAR']  th['BG_CARD']  th['FG_MAIN']  th['FG_ACCENT']
  th['FG_MUTED']    th['DIVIDER']  th['BG_CONTENT']  th['BG_SEL']
  th['insertbackground'] → use th['FG_ACCENT']
  th['selectbackground'] → use th['BG_SEL']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 _notes_open_editor 为左右分栏 WYSIWYG 布局</name>
  <files>ui/panel.py</files>
  <action>
**总体思路**

用一个新的 `self._notes_edit_pane`（tk.Frame）替代直接 pack `self._notes_text`。
分栏结构：
```
self._notes_body
└── self._notes_edit_pane  (pack fill=BOTH expand=True)
    ├── left_frame  (pack side=LEFT fill=BOTH expand=True)
    │   ├── fmt_bar  (高 36px, fill=X) — 格式工具栏
    │   │   └── 7 个 tk.Label 按钮：B  I  U  H1  H2  •  1.
    │   └── self._notes_text  (pack fill=BOTH expand=True) — 原有编辑器
    ├── tk.Frame(divider, width=1, bg=th['DIVIDER'])  (pack side=LEFT fill=Y)
    └── right_frame  (pack side=LEFT fill=BOTH expand=True)
        └── self._notes_preview  (tk.Text, DISABLED, 只读预览)
```

**修改步骤**

1. **在 `_build_notes_tab` 中**：
   - 删除 `self._notes_preview_btn` 的创建和所有绑定（约 2159–2162 行）。
   - 在同一函数末尾（widget 创建区）初始化 `self._notes_edit_pane = None` 和 `self._notes_preview = None`。

2. **重写 `_notes_open_editor(self, note_id)`**：

```python
def _notes_open_editor(self, note_id):
    self._notes_mode = 'edit'
    th = THEMES[self._theme_mode]
    notes = notes_service.load_all()

    if self._notes_list_frame:
        self._notes_list_frame.pack_forget()

    # 若旧的只读视图正在显示，隐藏 self._notes_text
    self._notes_text.pack_forget()

    # 清除并重建分栏容器（保证主题/内容刷新）
    if getattr(self, '_notes_edit_pane', None):
        self._notes_edit_pane.destroy()
        self._notes_edit_pane = None
        self._notes_preview = None

    self._notes_current_id = note_id
    content = ''
    if note_id is not None:
        for n in notes:
            if n['id'] == note_id:
                content = n['content']
                break
        self._notes_title_label.configure(text='编辑便签')
        self._notes_back_btn.pack(side=tk.LEFT, pady=4)
    else:
        self._notes_title_label.configure(text='新建便签')
        if notes:
            self._notes_back_btn.pack(side=tk.LEFT, pady=4)

    self._notes_save_btn.pack(side=tk.RIGHT, pady=4)
    self._notes_status.configure(text='')

    # ── 分栏容器 ──
    pane = tk.Frame(self._notes_body, bg=th['BG_CONTENT'])
    pane.pack(fill=tk.BOTH, expand=True)
    self._notes_edit_pane = pane

    # ── 左侧 ──
    left = tk.Frame(pane, bg=th['BG_CONTENT'])
    left.pack(side=tk.LEFT, fill=tk.BOTH, expand=True)

    # 格式工具栏（高 36px）
    fmt_bar = tk.Frame(left, bg=th['BG_TOOLBAR'], height=36)
    fmt_bar.pack(fill=tk.X)
    fmt_bar.pack_propagate(False)

    fmt_buttons = [
        ('B',  '**',   '**',   '**粗体**'),
        ('I',  '*',    '*',    '*斜体*'),
        ('U',  '<u>',  '</u>', '<u>下划线</u>'),
        ('H1', '# ',   '',     '# 标题1'),
        ('H2', '## ',  '',     '## 标题2'),
        ('•',  '- ',   '',     '- 列表项'),
        ('1.', '1. ',  '',     '1. 列表项'),
    ]

    for (label, prefix, suffix, placeholder) in fmt_buttons:
        btn = tk.Label(fmt_bar, text=label, bg=th['BG_TOOLBAR'],
                       fg=th['FG_MAIN'], font=('PingFang SC', 11),
                       cursor='hand2', padx=8, pady=4)
        btn.pack(side=tk.LEFT, padx=2)
        btn.bind('<Enter>', lambda e, b=btn: b.configure(fg=th['FG_ACCENT']))
        btn.bind('<Leave>', lambda e, b=btn: b.configure(fg=th['FG_MAIN']))
        btn.bind('<Button-1>',
                 lambda e, p=prefix, s=suffix, ph=placeholder:
                     self._notes_toolbar_wrap(p, s, ph))

    # 编辑器（复用已有的 self._notes_text）
    self._notes_text.configure(
        state=tk.NORMAL,
        bg=th['BG_CARD'], fg=th['FG_MAIN'],
        insertbackground=th['FG_ACCENT'],
        selectbackground=th['BG_SEL'],
    )
    self._notes_text.delete('1.0', tk.END)
    self._notes_text.insert('1.0', content)
    self._notes_text.pack(in_=left, fill=tk.BOTH, expand=True)

    # ── 垂直分隔线 ──
    tk.Frame(pane, bg=th['DIVIDER'], width=1).pack(side=tk.LEFT, fill=tk.Y)

    # ── 右侧预览 ──
    right = tk.Frame(pane, bg=th['BG_CARD'])
    right.pack(side=tk.LEFT, fill=tk.BOTH, expand=True)

    preview = tk.Text(right, bg=th['BG_CARD'], fg=th['FG_MAIN'],
                      font=('PingFang SC', 13),
                      relief=tk.FLAT, padx=16, pady=12,
                      wrap=tk.WORD, borderwidth=0,
                      state=tk.DISABLED)
    preview.pack(fill=tk.BOTH, expand=True)
    self._notes_preview = preview

    # 初次渲染预览
    self._render_markdown_in_widget(self._notes_preview, content)

    # 按键实时刷新预览
    def _refresh_preview(e=None):
        txt = self._notes_text.get('1.0', tk.END).rstrip('\n')
        self._render_markdown_in_widget(self._notes_preview, txt)

    self._notes_text.bind('<KeyRelease>', _refresh_preview)
    self._notes_text.focus_set()
```

3. **新增辅助方法 `_notes_toolbar_wrap`**（加在 `_notes_open_editor` 下方）：

```python
def _notes_toolbar_wrap(self, prefix: str, suffix: str, placeholder: str):
    """工具栏按钮点击：有选中则包裹，无选中则插入占位符。"""
    ed = self._notes_text
    try:
        sel_start = ed.index(tk.SEL_FIRST)
        sel_end   = ed.index(tk.SEL_LAST)
        selected  = ed.get(sel_start, sel_end)
        ed.delete(sel_start, sel_end)
        ed.insert(sel_start, prefix + selected + suffix)
    except tk.TclError:
        # 无选中：在光标处插入占位符
        cursor = ed.index(tk.INSERT)
        ed.insert(cursor, placeholder)
    ed.focus_set()
    # 触发预览刷新
    ed.event_generate('<KeyRelease>')
```

4. **修改 `_notes_show_list`**：
   - 在 `self._notes_text.pack_forget()` 之前/之后，增加：
     ```python
     if getattr(self, '_notes_edit_pane', None):
         self._notes_edit_pane.destroy()
         self._notes_edit_pane = None
         self._notes_preview = None
     ```
   - 删除或保留 `self._notes_preview_btn.pack_forget()` 和 `self._notes_previewing = False`（preview_btn 已被删除，这些行也一并清理掉）。

5. **修改 `_notes_open_readonly`**：
   - 在 `self._notes_text.pack(fill=tk.BOTH, expand=True)` 前加：
     ```python
     if getattr(self, '_notes_edit_pane', None):
         self._notes_edit_pane.destroy()
         self._notes_edit_pane = None
         self._notes_preview = None
     ```
   - 删除 `self._notes_preview_btn.pack_forget()` 这行（preview_btn 已不存在）。

6. **删除 `_notes_toggle_preview` 方法**（已无用）。

7. **在 `_build_notes_tab` 末尾（widget 初始化区）** 新增两行初始化：
   ```python
   self._notes_edit_pane = None
   self._notes_preview   = None
   ```
   并删除 `self._notes_previewing = False` 这行。

**重要：不要碰 `_notes_open_readonly`、`_strip_markdown`、`_render_markdown_in_widget` 的其他逻辑。**
  </action>
  <verify>
    <automated>/Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast, sys; ast.parse(open('/Users/maxinyue09/.openclaw/workspace/desktop-pet/ui/panel.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - ui/panel.py 语法检查通过
    - `_notes_open_editor` 包含 `_notes_edit_pane` 分栏容器的创建逻辑
    - `_notes_toolbar_wrap` 方法存在
    - `_notes_preview_btn` 不再被 pack（或已从 `_build_notes_tab` 删除）
    - `_notes_toggle_preview` 方法已删除
    - `_notes_open_readonly` 中增加了 `_notes_edit_pane` 清理逻辑
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    便签 WYSIWYG 分栏编辑器：
    - 编辑视图左右分栏（各约 50%），左侧顶部格式工具栏 + 编辑框，右侧实时预览
    - 7 个格式按钮（B / I / U / H1 / H2 / 无序 / 有序），有选中包裹，无选中插入占位符
    - 按键后预览实时更新
    - 日记只读视图保持原样
  </what-built>
  <how-to-verify>
    启动应用：
    `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 /Users/maxinyue09/.openclaw/workspace/desktop-pet/main.py`

    1. 打开主面板 → 便签 Tab → 新建便签
       - 预期：出现左右分栏，左侧顶部有 B I U H1 H2 • 1. 七个按钮，右侧空白预览
       - 预期：工具栏不再有「👁 预览 / ✏️ 编辑」按钮

    2. 在左侧输入 `# 标题` 后按任意键
       - 预期：右侧预览立即显示渲染后的蓝色大标题

    3. 输入一行普通文字，选中部分文字后点击 B
       - 预期：选中文字被 `**...**` 包裹，右侧预览同步加粗

    4. 将光标置于行尾（不选中），点击 H1
       - 预期：光标处插入 `# 标题1` 占位符，预览同步刷新

    5. 保存便签后返回列表，再点击一张便签重新进入编辑
       - 预期：分栏正常，已保存内容在左侧编辑框，右侧立即预览

    6. 点击一张日记卡片
       - 预期：日记只读视图正常展开（全宽、无工具栏、无分栏），内容渲染正确
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
- [ ] `python3.11 -c "import ast; ast.parse(open('ui/panel.py').read())"` 无报错
- [ ] 便签编辑视图为左右分栏，不含「👁 预览 / ✏️ 编辑」按钮
- [ ] 格式工具栏 7 个按钮正常显示和工作
- [ ] 实时预览随输入更新
- [ ] 日记只读视图不受影响
</verification>

<success_criteria>
- 打开编辑便签视图：左侧编辑框 + 右侧实时预览并排（~50/50），顶部 7 个格式按钮
- 按键后右侧预览刷新，无需手动切换
- 格式按钮：有选中则包裹，无选中则插入占位符
- 日记只读视图外观行为与重构前相同
</success_criteria>

<output>
完成后在 `.planning/quick/260424-g7w-markdown-wysiwyg/` 创建 `260424-g7w-SUMMARY.md`，记录：
- 修改了哪些方法（新增/删除/修改）
- 新增的实例属性（`_notes_edit_pane`、`_notes_preview`）
- 删除的属性/方法（`_notes_previewing`、`_notes_toggle_preview`、`_notes_preview_btn`）
</output>
