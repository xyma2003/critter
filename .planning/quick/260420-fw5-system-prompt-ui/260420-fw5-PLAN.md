---
phase: 260420-fw5-system-prompt-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop_pet.py
  - settings.json
autonomous: true
requirements:
  - FW5-name
  - FW5-personality
  - FW5-catchphrase
must_haves:
  truths:
    - "设置 Tab 有名字输入框、性格单选行、口头禅输入框，保存后写入 settings.json"
    - "宠物 Tab 顶部 emoji 下方显示宠物名字标签"
    - "_stream_pet_ai system prompt 包含名字、性格和口头禅"
  artifacts:
    - path: "desktop_pet.py"
      provides: "宠物自定义设置 UI + AI system prompt 注入"
    - path: "settings.json"
      provides: "持久化 pet_name / pet_personality / pet_catchphrase"
  key_links:
    - from: "_build_settings_tab"
      to: "_save_settings"
      via: "self._pet_name_var / self._personality_var / self._catchphrase_var"
    - from: "_stream_pet_ai"
      to: "self.pet.settings"
      via: "pet_name, pet_personality, pet_catchphrase keys"
---

<objective>
在 Critter 桌面宠物应用中增加三项宠物自定义设置：名字、性格、口头禅。
这三项通过 settings.json 持久化，名字在宠物 Tab 显示，三者均注入 AI 对话 system prompt。

Purpose: 让宠物有独特身份，AI 回复更贴合用户设定的人设。
Output: 更新后的 desktop_pet.py，settings.json 增加三个新字段。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Single-file architecture: all changes in desktop_pet.py.
Use Edit tool only (never sed). 4-space indent. Font: ('PingFang SC', SIZE).
Theme dict: th = THEMES[self._theme_mode]. Theme-aware colors from th['KEY'].

Key line references (desktop_pet.py):
- _stream_pet_ai system prompt: line 1343–1349
- _build_pet_tab emoji label: line 2085–2093
- _build_settings_tab card1 section: line 2498–2518
- _save_settings: line 2533–2542
</context>

<tasks>

<task type="auto">
  <name>Task 1: 设置 Tab 添加名字 / 性格 / 口头禅三行 UI</name>
  <files>desktop_pet.py</files>
  <action>
在 `_build_settings_tab` 中，`card1 = _section('宠物形象')` 之后（即 save_btn 之前），
新增一个 section 卡片 `card2 = _section('宠物性格')`，依次排列三行：

**Row A — 宠物名字**（text entry）：
```python
row_name = tk.Frame(card2, bg=th['BG_CARD'])
row_name.pack(fill=tk.X, padx=16, pady=12)
tk.Label(row_name, text='名字', bg=th['BG_CARD'], fg=th['FG_MUTED'],
         font=('PingFang SC', 11), width=8, anchor='w').pack(side=tk.LEFT)
self._pet_name_var = tk.StringVar(value=self.pet.settings.get('pet_name', '小猫'))
tk.Entry(row_name, textvariable=self._pet_name_var, width=14,
         bg=th['BG_HOVER'], fg=th['FG_MAIN'], relief=tk.FLAT,
         font=('PingFang SC', 11),
         insertbackground=th['FG_MAIN']).pack(side=tk.LEFT)
```

**Divider line** between rows (height=1, bg=th['DIVIDER'], fill=tk.X, padx=16).

**Row B — 宠物性格**（5-option radio buttons displayed as Label buttons）：
```python
row_personality = tk.Frame(card2, bg=th['BG_CARD'])
row_personality.pack(fill=tk.X, padx=16, pady=12)
tk.Label(row_personality, text='性格', bg=th['BG_CARD'], fg=th['FG_MUTED'],
         font=('PingFang SC', 11), width=8, anchor='w').pack(side=tk.LEFT)
self._personality_var = tk.StringVar(
    value=self.pet.settings.get('pet_personality', '温柔'))
for opt in ['温柔', '活泼', '傲娇', '淡定', '搞笑']:
    b = tk.Label(row_personality, text=opt,
                 font=('PingFang SC', 11), cursor='hand2',
                 padx=10, pady=4,
                 highlightthickness=1)
    b.pack(side=tk.LEFT, padx=3)
    # Color helper: selected = accent bg, unselected = card bg
    def _update_personality_btns():
        for child in row_personality.winfo_children():
            if isinstance(child, tk.Label) and child.cget('text') in ['温柔','活泼','傲娇','淡定','搞笑']:
                if child.cget('text') == self._personality_var.get():
                    child.configure(bg=th['FG_ACCENT'], fg='#ffffff',
                                    highlightbackground=th['FG_ACCENT'])
                else:
                    child.configure(bg=th['BG_CARD'], fg=th['FG_MAIN'],
                                    highlightbackground=th['BORDER'])
    def _on_personality_click(opt=opt):
        self._personality_var.set(opt)
        _update_personality_btns()
    b.bind('<Button-1>', lambda e, o=opt: _on_personality_click(o))
_update_personality_btns()
```

**Divider line** between rows.

**Row C — 口头禅**（text entry）：
```python
row_catchphrase = tk.Frame(card2, bg=th['BG_CARD'])
row_catchphrase.pack(fill=tk.X, padx=16, pady=12)
tk.Label(row_catchphrase, text='口头禅', bg=th['BG_CARD'], fg=th['FG_MUTED'],
         font=('PingFang SC', 11), width=8, anchor='w').pack(side=tk.LEFT)
self._catchphrase_var = tk.StringVar(
    value=self.pet.settings.get('pet_catchphrase', '喵~'))
tk.Entry(row_catchphrase, textvariable=self._catchphrase_var, width=20,
         bg=th['BG_HOVER'], fg=th['FG_MAIN'], relief=tk.FLAT,
         font=('PingFang SC', 11),
         insertbackground=th['FG_MAIN']).pack(side=tk.LEFT)
```

Place `card2` block immediately after the existing `card1` block (emoji row), before `save_btn`.

The `_update_personality_btns` helper must be defined before the for-loop so it can be called at the end of the loop to set initial highlight state.
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast; ast.parse(open('desktop_pet.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    settings Tab 有"宠物性格"section，包含名字文字框、5-项性格按钮（当前选中高亮）、口头禅文字框。
    语法检查通过，无 SyntaxError。
  </done>
</task>

<task type="auto">
  <name>Task 2: 保存逻辑 + 宠物 Tab 名字显示 + AI system prompt 注入</name>
  <files>desktop_pet.py</files>
  <action>
**Step A — 更新 `_save_settings`（line ~2533）**

在现有 `s['pet_emoji'] = self._emoji_var.get()` 行之后追加三行：
```python
s['pet_name']        = self._pet_name_var.get().strip() or '小猫'
s['pet_personality'] = self._personality_var.get()
s['pet_catchphrase'] = self._catchphrase_var.get().strip() or '喵~'
```
同时在保存后刷新宠物名字标签（如果存在）：
```python
if hasattr(self, '_pet_name_display_lbl'):
    self._pet_name_display_lbl.configure(
        text=s.get('pet_name', '小猫'))
```

**Step B — 在宠物 Tab 显示名字（`_build_pet_tab` line ~2090）**

在 `self._pet_mood_lbl` pack 之后（即现有心情标签下方），插入宠物名字标签：
```python
self._pet_name_display_lbl = tk.Label(
    left,
    text=self.pet.settings.get('pet_name', '小猫'),
    bg=th['BG_CONTENT'], fg=th['FG_MUTED'],
    font=('PingFang SC', 11))
self._pet_name_display_lbl.pack(pady=(0, 4))
```
顺序：emoji_label → mood_lbl → **name_display_lbl** → btn_frame

**Step C — 更新 `_stream_pet_ai` system prompt（line 1343–1349）**

将现有：
```python
system = (
    f'你是一只可爱的桌面宠物 {emoji}，性格温柔活泼，说话简短可爱，'
    '偶尔用叠词或语气词，回复控制在 2-3 句以内，不要用 Markdown 格式。'
    + self.stats.system_prompt_hint()
)
```
替换为：
```python
pet_name        = self.pet.settings.get('pet_name', '小猫')
pet_personality = self.pet.settings.get('pet_personality', '温柔')
pet_catchphrase = self.pet.settings.get('pet_catchphrase', '喵~')
system = (
    f'你是一只可爱的桌面宠物 {emoji}，名字叫{pet_name}，'
    f'性格{pet_personality}，说话简短可爱，'
    f'偶尔使用你的口头禅"{pet_catchphrase}"，'
    '偶尔用叠词或语气词，回复控制在 2-3 句以内，不要用 Markdown 格式。'
    + self.stats.system_prompt_hint()
)
```

Use Edit tool for each of the three targeted changes. Do NOT rewrite unrelated sections.
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "import ast; ast.parse(open('desktop_pet.py').read()); print('syntax OK')" && python3.11 -c "import json; s=json.load(open('settings.json')); print('settings OK')"</automated>
  </verify>
  <done>
    - 保存设置后 settings.json 包含 pet_name / pet_personality / pet_catchphrase 三个键
    - 宠物 Tab emoji 下方出现名字标签（默认"小猫"）
    - _stream_pet_ai system prompt 字符串包含名字、性格、口头禅三项内容
    - 语法检查通过
  </done>
</task>

</tasks>

<verification>
1. Python 语法检查：`python3.11 -c "import ast; ast.parse(open('desktop_pet.py').read())"`
2. 运行应用，打开设置 Tab，可见"宠物性格"section 含三行控件
3. 修改名字为"阿橘"，性格选"傲娇"，口头禅改"哼~"，保存
4. 切换到宠物 Tab，左侧 emoji 下方显示"阿橘"
5. 与宠物对话，回复风格体现傲娇性格和"哼~"口头禅
</verification>

<success_criteria>
- settings.json 保存后包含 pet_name、pet_personality、pet_catchphrase 三个字段
- 宠物 Tab 宠物名字标签与设置中名字同步
- AI 对话 system prompt 动态包含名字、性格、口头禅
- 整个文件 Python 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/quick/260420-fw5-system-prompt-ui/260420-fw5-SUMMARY.md`
</output>
