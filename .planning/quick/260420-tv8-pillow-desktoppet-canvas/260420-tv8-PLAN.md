---
phase: quick
plan: 260420-tv8
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop_pet.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "桌面悬浮猫显示为可爱正脸卡通猫（橙黄色，大圆眼，腮红，三角耳）"
    - "idle 状态猫身体有轻微左右摆动动画（4 帧循环）"
    - "每 3-5 秒触发一次眨眼帧"
    - "set_emoji() 调用后正确切换表情帧（happy/sleepy/excited/normal）"
    - "trigger_bounce() 和 set_emoji() 接口行为不变"
    - "透明背景正常（猫之外区域不显示白色/黑色矩形）"
  artifacts:
    - path: "desktop_pet.py"
      provides: "_generate_cat_frames() 方法 + 替换后的 _draw_cat() 逻辑"
      contains: "_generate_cat_frames"
  key_links:
    - from: "DesktopPet.__init__"
      to: "_generate_cat_frames()"
      via: "启动时调用，帧存入 self._cat_frames"
    - from: "DesktopPet._animate()"
      to: "canvas.create_image()"
      via: "用 ImageTk.PhotoImage 帧替代 _draw_cat() Canvas 绘制"
---

<objective>
用 Pillow 在内存生成可爱正脸卡通猫 PNG 帧集，替换 DesktopPet 中现有的 Canvas 向量猫绘制逻辑。

Purpose: 当前向量猫视觉效果简陋；Pillow 可绘制扁平卡通风格、大圆眼双高光、腮红、轮廓线的高质量猫形象。
Output: 修改后的 desktop_pet.py，猫形象显著改善，动画帧/心情帧完整，接口保持不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/maxinyue09/.openclaw/workspace/desktop-pet/desktop_pet.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 Pillow 导入 + _generate_cat_frames() 方法</name>
  <files>desktop_pet.py</files>
  <action>
**Step 1 — 添加 Pillow 导入（文件顶部 import 区域，约第 18 行后追加）：**

```python
try:
    from PIL import Image, ImageDraw, ImageTk
    _PIL_AVAILABLE = True
except ImportError:
    _PIL_AVAILABLE = False
```

**Step 2 — 在 DesktopPet 类中（`set_emoji` 方法之前，约 2800 行附近）添加 `_generate_cat_frames()` 方法：**

方法签名：`def _generate_cat_frames(self):`

该方法返回一个 dict，结构为：
```python
{
    'idle':    [img0, img1, img2, img3],  # 4帧，身体±3px左右摆动
    'blink':   [img_blink],               # 1帧，眼睛半闭
    'happy':   [img_happy],               # 1帧，弯弯笑眼
    'excited': [img_excited],             # 1帧，大眼+张嘴
    'sleepy':  [img_sleepy],              # 1帧（同 blink）
}
```

每帧用 `Image.new('RGBA', (size, size), (0,0,0,0))` 创建透明背景，`ImageDraw.Draw(img)` 绘制。`size = self.w`（通常为 96px）。

**绘制规格（以 96px 为基准，按比例 s = size/96 缩放所有坐标）：**

猫的中心坐标 cx=size//2, cy=size//2+2

1. **三角耳（先画，让头圆覆盖底部）**
   - 左耳：多边形顶点 [(cx-28,cy-28), (cx-38,cy-55), (cx-12,cy-50)]，填色 #F5A623，黑色轮廓 width=2
   - 右耳：多边形顶点 [(cx+28,cy-28), (cx+38,cy-55), (cx+12,cy-50)]，同色
   - 左耳内：[(cx-27,cy-30), (cx-35,cy-50), (cx-15,cy-46)]，填色 #FF8FAB（玫瑰粉），无轮廓
   - 右耳内：对称

2. **头圆**
   - 椭圆 bounding box: (cx-30, cy-32, cx+30, cy+26)，填色 #F5A623，黑色轮廓 width=2

3. **腮红**
   - 左：椭圆 (cx-26, cy-2, cx-12, cy+10)，填色 #FFB3C6，alpha=180（用 RGBA tuple (255,179,198,180)），无轮廓
   - 右：对称 (cx+12, cy-2, cx+26, cy+10)

4. **眼睛（normal 帧）**
   - 左眼中心 (cx-13, cy-10)，右眼中心 (cx+13, cy-10)
   - 外白圆：半径 9，填色白色，黑色轮廓 width=1
   - 瞳孔（深绿 #2D8A4E）：半径 6，居中
   - 瞳仁（#1A1A2E）：半径 3，位于瞳孔中偏下 (+1,+1)
   - 高光1（白色）：半径 2，位于瞳孔中 (-3,-3) 偏移处
   - 高光2（白色）：半径 1，位于瞳孔中 (+2,-1) 偏移处

5. **鼻子**
   - 大椭圆：bounding box (cx-5, cy+2, cx+5, cy+10)，填色 #3D2B1F（深棕），黑色轮廓 width=1
   - 高光小椭圆：(cx-3, cy+3, cx, cy+6)，填色白色，无轮廓

6. **嘴**
   - normal：两条曲线弧 — 从鼻子底部 (cx, cy+10) 分别向左下 (cx-8, cy+16) 和右下 (cx+8, cy+16)，用 `draw.line()` 绘制，颜色 #C0506A，width=2

7. **胡须（6 根）**
   - 左侧 3 根：从 (cx-7, cy+6) 出发，向左延伸到 (cx-26, cy+4)/(cx-25, cy+7)/(cx-24, cy+10)，颜色 #888888，width=1
   - 右侧 3 根：对称

8. **idle 摆动（4 帧）：**
   - 帧 0: body_offset_x=0，帧 1: body_offset_x=+3，帧 2: body_offset_x=0，帧 3: body_offset_x=-3
   - 所有元素整体平移 body_offset_x（猫整体左右微摆）

9. **blink/sleepy 帧：** 眼睛改为半闭弧线 — 用 `draw.arc(bbox, 0, 180, fill=None, outline='#2D8A4E', width=2)` 绘制上半圆弧，眼白椭圆仍保留但高度缩小一半

10. **happy 帧：** 眼睛改为弯弯笑眼 — 用 `draw.arc(bbox, 180, 360, fill=None, outline='#2D8A4E', width=2)`（下弧，呈 U 形弧倒置 → 实为 arc start=180 extent=180 → ∪ 形，正确写法为绘制下半弧 start=0 extent=-180 ≡ 绘制上凸弧，需测试）；更简单：画一段 chord 弧，bbox 为 (cx-13-9, cy-10-9, cx-13+9, cy-10+9)，start=200, end=340

11. **excited 帧：** 
    - 眼睛：外白圆半径 11（比 normal 大 2px），瞳孔更大
    - 嘴：加一个椭圆张开嘴 (cx-6, cy+10, cx+6, cy+18)，填色 #C0506A

12. **返回前将每帧转换为 ImageTk.PhotoImage：**
    ```python
    frames[key] = [ImageTk.PhotoImage(img) for img in raw_frames[key]]
    ```
    注意：ImageTk.PhotoImage 必须在 Tk 主循环存在后创建，所以 `_generate_cat_frames()` 在 `__init__` 里 `self.canvas.pack()` 之后调用。

**重要：** 若 `_PIL_AVAILABLE` 为 False，方法返回 `None`，后续代码回退到原始 `_draw_cat()`。
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "
import ast, sys
with open('desktop_pet.py') as f:
    src = f.read()
tree = ast.parse(src)
methods = [n.name for cls in ast.walk(tree) if isinstance(cls, ast.ClassDef) and cls.name=='DesktopPet' for n in cls.body if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
assert '_generate_cat_frames' in methods, '_generate_cat_frames missing'
assert '_PIL_AVAILABLE' in src, 'PIL import guard missing'
print('OK: _generate_cat_frames found, PIL import guard found')
"
</automated>
  </verify>
  <done>_generate_cat_frames() 存在于 DesktopPet 类中；PIL 导入带 try/except 保护；语法无报错。</done>
</task>

<task type="auto">
  <name>Task 2: 替换 _animate() 中的绘制调用，集成帧动画</name>
  <files>desktop_pet.py</files>
  <action>
**修改 DesktopPet.__init__ 中：**

在 `self.canvas.pack()` 之后、`self._animate()` 之前，添加：

```python
# 生成 Pillow 帧（需在 Tk 根窗口创建后）
if _PIL_AVAILABLE:
    self._cat_frames = self._generate_cat_frames()
    self._idle_frame_idx = 0
    self._blink_cooldown = 0      # 当前距下次眨眼的帧数
    self._blink_active = False    # 正在显示眨眼帧
    self._blink_duration = 0      # 眨眼帧剩余显示帧数
    self._cat_image_id = None     # canvas image item id
else:
    self._cat_frames = None
```

**修改 `_animate()` 方法：**

在现有 `_animate()` 方法开头（offset_y/scale 计算完成后），将原来的 `self._draw_cat(offset_y, scale)` 调用替换为：

```python
if self._cat_frames:
    self._draw_cat_pillow(offset_y)
else:
    self._draw_cat(offset_y, scale)
```

**新增 `_draw_cat_pillow(self, offset_y)` 方法（放在 `_draw_cat` 方法之前）：**

```python
def _draw_cat_pillow(self, offset_y):
    """使用预生成 Pillow 帧渲染猫，替代 Canvas 向量绘制。"""
    mood = self._cat_mood
    if self._mood_timer > 0:
        self._mood_timer -= 1
    else:
        mood = 'normal'
        self._cat_mood = 'normal'

    # 眨眼计时
    self._blink_timer += 1
    if self._blink_active:
        self._blink_duration -= 1
        if self._blink_duration <= 0:
            self._blink_active = False
            self._blink_cooldown = random.randint(60, 100)  # 3-5秒@50ms
    else:
        if self._blink_cooldown > 0:
            self._blink_cooldown -= 1
        else:
            self._blink_active = True
            self._blink_duration = 4  # 4帧眨眼持续

    # 选择帧
    if self._blink_active or mood == 'sleepy':
        frames = self._cat_frames.get('blink') or self._cat_frames['idle']
        frame = frames[0]
    elif mood == 'happy':
        frames = self._cat_frames.get('happy') or self._cat_frames['idle']
        frame = frames[0]
    elif mood == 'excited':
        frames = self._cat_frames.get('excited') or self._cat_frames['idle']
        frame = frames[0]
    else:
        # idle 4帧循环
        self._idle_frame_idx = (self._anim_frame // 6) % 4
        frames = self._cat_frames['idle']
        frame = frames[self._idle_frame_idx]

    # 渲染到 Canvas
    self.canvas.delete('all')
    cx = self.w // 2
    cy = self.h // 2 + offset_y
    if self._cat_image_id is None:
        self._cat_image_id = self.canvas.create_image(cx, cy, image=frame, anchor='center')
    else:
        self.canvas.itemconfig(self._cat_image_id, image=frame)
        self.canvas.coords(self._cat_image_id, cx, cy)
    # 保持引用防止 GC
    self.canvas._pillow_frame_ref = frame
```

**修改 set_emoji() 方法** — 扩展心情映射以覆盖任务要求的 emoji：

将现有 `mood_map` dict 替换为：
```python
mood_map = {
    '😊': 'happy',  '🙂': 'normal', '😐': 'normal',
    '😔': 'sleepy', '😞': 'sleepy', '😴': 'sleepy',
    '😋': 'happy',  '😹': 'excited','😸': 'happy',
    '😺': 'happy',  '😍': 'excited','🎉': 'excited',
}
```
  </action>
  <verify>
    <automated>cd /Users/maxinyue09/.openclaw/workspace/desktop-pet && /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -c "
import ast
with open('desktop_pet.py') as f:
    src = f.read()
tree = ast.parse(src)
methods = [n.name for cls in ast.walk(tree) if isinstance(cls, ast.ClassDef) and cls.name=='DesktopPet' for n in cls.body if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
assert '_draw_cat_pillow' in methods, '_draw_cat_pillow missing'
assert '_cat_frames' in src, '_cat_frames missing'
assert \"'😸': 'happy'\" in src or '\"😸\": \"happy\"' in src, 'emoji mapping missing'
print('OK: _draw_cat_pillow found, _cat_frames ref found, emoji mapping found')
"
</automated>
  </verify>
  <done>
  - `_draw_cat_pillow()` 方法存在且语法正确
  - `_animate()` 中根据 `_PIL_AVAILABLE` 分支调用 Pillow 路径或旧路径
  - `set_emoji()` 中 😸/😺/😍/🎉 映射正确
  - 启动 app 后桌面显示橙黄色正脸卡通猫，眼睛/腮红/耳朵清晰可见
  - 透明背景正常（猫外区域完全透明）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Pillow 卡通猫帧生成 + Canvas image 渲染替换完成。DesktopPet 在启动时调用 _generate_cat_frames()，_animate() 改用 _draw_cat_pillow()。
  启动命令：/Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 /Users/maxinyue09/.openclaw/workspace/desktop-pet/desktop_pet.py
  </what-built>
  <how-to-verify>
  1. 运行启动命令，观察桌面右下角悬浮猫
  2. 确认猫形象：橙黄色正脸，大圆眼睛（带白色高光），深色椭圆鼻子，玫瑰色腮红，三角耳带粉色内耳
  3. 确认 idle 动画：猫身体有轻微左右摆动（每约 0.3s 一次细微摆动）
  4. 等待约 5 秒，确认自动眨眼（眼睛短暂半闭后恢复）
  5. 点击猫打开主面板，在宠物 tab 点击心情 emoji（😸、😍、😴），确认猫表情切换
  6. 确认猫外区域完全透明（无白色/黑色矩形背景）
  </how-to-verify>
  <resume-signal>输入 "approved" 确认效果满意，或描述具体问题（如哪个帧异常、透明度问题等）</resume-signal>
</task>

</tasks>

<verification>
- desktop_pet.py 语法正确：`python3.11 -c "import ast; ast.parse(open('desktop_pet.py').read()); print('OK')`
- PIL 导入有 try/except 保护（_PIL_AVAILABLE fallback）
- `_generate_cat_frames`, `_draw_cat_pillow` 均在 DesktopPet 类中
- `_animate()` 保留 scale/offset_y 逻辑（bounce/hover 仍工作），仅渲染调用改变
- `trigger_bounce()` 和 `set_emoji()` 对外接口签名不变
</verification>

<success_criteria>
- 桌面猫显示为可爱橙黄色正脸卡通风格（扁平设计，大圆眼双高光，腮红，三角耳）
- idle 摆动动画流畅，无闪烁
- 心情切换（happy/excited/sleepy）视觉上有明显区别
- 自动眨眼每 3-5 秒触发
- 透明背景完全正常
- Pillow 不可用时优雅回退到旧向量猫
</success_criteria>

<output>
完成后创建 /Users/maxinyue09/.openclaw/workspace/desktop-pet/.planning/quick/260420-tv8-pillow-desktoppet-canvas/260420-tv8-SUMMARY.md
</output>
