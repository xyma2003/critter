---
phase: quick/260420-tv8-pillow-desktoppet-canvas
task: in_progress
status: blocked_on_rendering
last_updated: 2026-04-21T03:54:57Z
---

<current_state>
悬浮宠物窗口透明背景已修好（白框消除），但宠物图像始终不显示。
已将形象从橘猫改为3D卡通浣熊（参考图：灰棕条纹、白胸腹、眼罩纹、举手站姿、环纹尾巴）。
渲染调试显示代码在正常执行 create_image，但屏幕上看不到图像。
</current_state>

<completed_work>
- 窗口尺寸从 96px → 200px（settings.json pet_size=200）
- 白色背景框问题已解决：canvas bg=systemTransparent，ctypes 只针对宠物窗口（windowNumber 匹配）
- 形象完全重设计：3D卡通浣熊（_generate_cat_frames 全部重写，line ~2894）
- 布局验证：耳顶14px>0，腿底594px<600px，本地PNG预览正常
</completed_work>

<remaining_work>
- 核心问题：宠物图像不显示在屏幕上（create_image 执行但不可见）
- 浣熊形象细节优化（待图像能显示后再调）
</remaining_work>

<decisions_made>
- Python: /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11
- 透明方案: tkinter -transparent True + canvas bg=systemTransparent + ctypes windowNumber 精确匹配
- 形象: 3D卡通浣熊（用户提供参考图）
- 尺寸: 200px
</decisions_made>

<blockers>
图像不显示，可能原因：
1. PhotoImage 在 __init__ 中创建（mainloop 前）在 macOS 上失效
2. systemTransparent canvas 上 PhotoImage 渲染问题
3. delete('all') 后 create_image 在透明 canvas 上异常

关键代码位置：
- DesktopPet.__init__: line ~2752
- _generate_cat_frames(): line ~2894
- _draw_cat_pillow(): line ~3129（delete all + create_image）
- _animate(): line ~3348
</blockers>

<next_action>
调试图像不显示，按优先级：

1. 延迟创建 PhotoImage：在 __init__ 先 self._cat_frames=None，用 root.after(300, self._init_pillow_frames) 延迟到 mainloop 后创建

2. 改用 itemconfig 更新：第一次 create_image，之后 canvas.itemconfig(id, image=frame) 而非每帧 delete+create

3. 检查窗口位置：print(self.root.winfo_x(), self.root.winfo_y(), self.root.winfo_width(), self.root.winfo_height())

4. 先测试在 systemTransparent canvas 上画矩形是否可见（排除 canvas 渲染问题）

启动命令：
pkill -f "desktop_pet.py" 2>/dev/null
cd ~/.openclaw/workspace/desktop-pet
/Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 desktop_pet.py > /tmp/pet.log 2>&1 &
</next_action>
