---
phase: quick/260420-tv8-pillow-desktoppet-canvas
task: done
total_tasks: 1
status: completed_with_issues
last_updated: 2026-04-21T02:44:46.811Z
---

<current_state>
Q版小猫已经可以在桌面右下角显示，但本次 session 主要解决了两个问题：
1. 透明窗口导致猫完全不可见（Canvas bg 改为 'white' 临时解决）
2. delete('all') 后 image id 失效导致猫不渲染（每帧重新 create_image 修复）
3. 耳朵从三角形改为半圆形（chord 绘制）

当前状态：猫可见，白色背景方块，半圆耳朵，大眼睛紫色虹膜。
</current_state>

<completed_work>

- Q版猫绘制逻辑重写：大圆头、半圆耳、大眼睛（紫色虹膜+高光）、粉鼻子、W嘴、腮红
- 修复 canvas bg 为 'white'（临时方案，透明背景问题未彻底解决）
- 修复 _draw_cat_pillow() 中每帧重建 image id 的 bug
- 耳朵从三角形改为半圆形（chord 绘制，更自然）
- 外层帧生成 sc(3) 改为 sc_outer(3)（修复 NameError）
</completed_work>

<remaining_work>

- **透明背景未恢复**：Canvas bg 目前是 'white'，原本应该是 'systemTransparent'
  - 问题根因：macOS tkinter `-transparent True` + Canvas RGBA 图像不能正确合成
  - 可能方案：用 NSWindow/ctypes 设置真正透明，或接受白色背景
- PLAN.md 中的 must_haves 里"透明背景正常"这条未达成
</remaining_work>

<decisions_made>

- 选择 chord（半圆）而非 polygon（三角）作为耳朵形状，更圆润自然
- 颜色方案：奶白色主体 #FEFEFE，紫色虹膜 #6B3FA0，粉色耳内 #FFB3C6
- 暂时用白色背景代替透明，让猫先能显示出来
- 每帧重新 create_image（而非 itemconfig）解决 delete('all') 后 id 失效问题
</decisions_made>

<blockers>

- 透明背景问题：macOS tkinter 的 `-transparent True` 会让整个 Canvas 内容也穿透，
  Pillow RGBA 图像无法正确合成到透明窗口上。
  已尝试：`-transparentcolor`（macOS 不支持）、`systemTransparent`（全透明）、`white`（当前临时方案）
  下一步可尝试：ctypes NSWindow setOpaque/setBackgroundColor，或用 Quartz 合成
</blockers>

<context>
这是 quick task tv8，目标是用 Pillow 替换 Canvas 向量猫。
核心功能（猫的绘制、帧动画、心情切换）已实现，
但透明背景这个 must_have 条件未完全满足（当前是白色方块背景）。
用户对耳朵形状有反馈，已从三角改为半圆，用户看到了改进。
</context>

<next_action>
解决透明背景问题：
1. 尝试 ctypes 调用 NSWindow 的 setOpaque(False) + setBackgroundColor(NSColor.clearColor())
2. 参考 desktop_pet.py 中已有的 _fix_panel_window_level 方法（ctypes NSWindow 调用示例）
3. 在 DesktopPet.__init__ 中，Canvas bg 恢复为透明色，同时用 NSWindow API 设置窗口真正透明
</next_action>
